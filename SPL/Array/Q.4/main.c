#include<stdio.h>

int main()
{
    char ch;
    int i,shift_value;

    printf("Shift value:");
    scanf("%d",&shift_value);
    printf("Message :");
   scanf("%c",&ch);
     scanf(" %c",&ch);
        //  printf("\nEnter %d character : ",i);
        scanf(" %c",&ch);
scanf("%c",&ch);
     scanf(" %c",&ch);
scanf(" %c",&ch);
scanf("%c",&ch);
     scanf(" %c",&ch);
scanf(" %c",&ch);
scanf("%c",&ch);
     scanf(" %c",&ch);
      if(ch==' ')
        printf(" ");
        if (ch >= 'A' && ch <= 'Z')
        {

            printf("%c",ch-shift_value);

        }

    return 0;
}


/*
int main()
{
    char ch;
    int i,J,shift_value;


    printf("Message :");

    for(i=1,J=1; i<=30; i++,J++)
    {

        //  printf("\nEnter %d character : ",i);
        scanf("%c",&ch);

        if(ch=='  ')
            printf("%c",ch);
        if(ch>='D' )
         if(ch=='f')
        {
           printf("Shift value:");
           scanf("%d",&shift_value);
        }
            printf("%c",ch-shift_value);

    }
    return 0;
}
*/
